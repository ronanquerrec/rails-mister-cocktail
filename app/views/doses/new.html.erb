
      <h1>Add doses to cocktail</h1>

      <%= simple_form_for [@cocktail, @dose] do |f| %>
        <%= f.input :description %>
        <%= f.association :ingredient %>

        <%= f.submit class: 'btn btn-primary' %>
      <% end %>

      <%= link_to "See all cocktails", cocktails_path %>

